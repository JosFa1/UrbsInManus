{
  "name": "urbs-in-manus",
  "version": "1.0.0",
  "description": "Roman City Builder Game",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run postbuild",
    "postbuild": "copy buildingCatalog.js dist && copy saveAdapter.js dist && copy buildTool.js dist && copy game.js dist && copy map.png dist && copy favicon.ico dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "keywords": [
    "game",
    "roman",
    "city-builder"
  ],
  "author": "",
  "license": "MIT"
}
